# 任务进度

> 本文档维护当前任务进度，供 agent 在开始非简单任务前读取。
> 只保留当前有效信息，不写成无限追加日志。
> 出现阶段完成、方向变化、失败尝试、准备结束会话时，必须更新本文件。

## 当前任务
**无进行中的开发任务。** 最后一轮开发（2026-09-20 上位机 BLE 链路）已实机验证通过并收尾。
用户新提出「整理本文件夹下的内容」，**整理范围尚未确认，任务挂起**，见待办第 1 条 —— 动手前必须先拿到用户对三个选项的答复。

## 成功标准
（针对挂起的整理任务）
根目录只保留性质清晰的分类内容：源码/工程与可再生的编译产物、编辑器缓存、日志彼此分开；每个目录与关键文件有可读的用途说明；不改动任何源码语义、整理后 Keil 工程仍能编译出 `0 Error(s), 0 Warning(s)`。

## 范围边界
- 本轮**不做**：不改固件功能与协议、不改上位机逻辑、不重命名 `.uvprojx` 工程名、不初始化 git、不推送远端。
- 整理只涉及文件位置与说明文档；任何删除/移动用户文件前必须先取得确认。

## 停止条件
<!-- 什么情况下应暂停并汇报 -->
- 涉及删除或移动文件，但用户未确认范围 → 停止并汇报，不得自行决定。
- 改固件后编译失败或出现 Warning → 停止，不得带着错误状态继续。
- 硬件相关结论未经上机实测 → 不得作为事实写入文档，只能记为待验证。
- 出现与当前任务无关的缺陷 → 只记录到 bugs.md，不顺手重构或扩大范围。

## 已完成
### 下位机改造（2026-09-19）
- 从教程源码复制出 `6-5 PWM驱动直流电机-改造版` 并按工程规范整理（User / Hardware / System / Start / Library / Objects / Listings）。
- 按键、LED、OLED、PWM、Serial（USART1）驱动接入；主循环 10ms 节拍。
- 实现档位调速：`MOTOR_GEAR_MAX=50`，`Motor_GetDuty()` 按档位换算占空比，PC 端只发档位不发占空比。
- 实现文本行命令解析（大小写不敏感）：`RUN/SPD/STOP/STATUS/?/PING/TUNE`，回 `READY/OK/DONE/ERR/STATUS`。
- 实现**启动助推**：仅从 `CurDuty==0` 启动时先给 65% 约 300ms 再降到目标档，`Motor_Tick()` 每 10ms 收尾。

### 蓝牙链路（2026-09-20）
- 用 AT 体检确认模块为克隆固件 `hc05V2.3_le`，并把 `ROLE=1`（主机模式，LED 照闪但搜不到）改回 `ROLE=0` + `AT+RESET`。
- 用设备枚举 + BLE 扫描定性：该模块是 **BLE-only**（`BTHLE\DEV_21F6473AD889` + FFE0/FFE1/FFE2），Windows 不会生成 COM 口，pyserial 路线不通。
- 新增 `motor_ble_gui.py`（bleak 实现）：界面/协议/固件代码全部沿用，仅替换底层通讯层；已实机验证全链路通。
  连接耗时 14.93s、协商 MTU 517、往返约 130ms；`RUN30,3000` → `+0.28s OK RUN 30 3000` / `+3.47s DONE 30 3000`。
- 排查工具落盘：`hc05_at_tool.py`（AT 体检/修复）、`_ble_probe.py`（BLE 扫描 + GATT 枚举）；`ble_config.json` 记住上次地址。

### TUNE 自动标定（2026-09-20）
- 实现 `TUNE` 状态机：65% 起 → 45% 以上每步降 5%/停 800ms → 45% 以下每步降 1%/停 900ms → 最低 13%。
- 用户按板上按键标记"停转点"，回 `TUNE MARK <duty>` 与 `TUNE DONE mark=.. floor=.. span=..`，并把 1 档下限临时改为 `mark + 3%` 余量（掉电复位）。
- 上位机 GUI 加「① 标定最低速」按钮；`span too small` 时提示用户调小 `MOTOR_GEAR_MAX`。

### 工具链与文档
- 固化命令行编译验证路径：`D:\Keil5\UV4\UV4.exe -b Project.uvprojx -j0 -o build_log.txt`（AC5，当前基线 0 Error 0 Warning）。
- 沉淀技能：`keil-uv4-cli-build`（命令行编译 Keil 工程 + GBK 编码坑）、`dc-motor-pwm-low-speed`（低速/静摩擦门槛 + 标定方法）、`hc05-classic-bt-troubleshoot`（经典蓝牙排查）。
- 建立本 `agent_memory/` 三文件（2026-09-20），作为后续会话的上下文入口。

## 待办
1. **【等待用户确认，勿自行动手】整理工作区目录**。待确认三点：
   (a) 整理力度：只加导航说明 / 归位产物与缓存日志 / 重组为「固件 + 上位机 + 文档」三大块 / 全都要；
   (b) Keil 工程里约 110 个可再生编译产物（`.o/.d/.crf/.map`）是原地不动、移到 `_归档/` 保留，还是直接清理；
   (c) 用途：仅本地清爽 / 上传 GitHub 存档（需加 `.gitignore` + README）/ 打包分发（需清个人痕迹）。
2. 把 `MOTOR_DUTY_MIN=280` 的实际手感复测一遍（1 档能否稳定维持），结果记入 bugs.md。
3. 用 `TUNE` 标定出真实 `floor`，据此决定 `MOTOR_GEAR_MAX` 是否需要调小，并同步改 PC 工具的同名常量。
4. `用户手册.pdf` 需要归类并明确是哪份手册（TB6612 / HC-05 / 其他）。
5. 可选：把 `motor_bt_gui.py`（串口备用版）与在用版的关系写进面向使用者的 README，避免以后误用。
6. 可选：为工程补一份 README + `.gitignore`（若后续要推 GitHub，先排除 `Objects/`、`Listings/`、`*.uvguix.*`、`build_log.txt`）。

## 阶段变更记录
- 2026-09-19：从教程源码复制并改造为"6-5 PWM驱动直流电机-改造版"；完成驱动接入、档位调速、串口命令协议，编译通过。
- 2026-09-20：定性并修复蓝牙问题（`ROLE=1` → `ROLE=0`；确认模块实为 BLE-only，Windows 无 COM 口）；改用 bleak 重写电脑端 GUI 并实机验证全链路通。
- 2026-09-20：新增 `TUNE` 自动标定最低可维持占空比；档位参数改为 `28.0% ~ 38.0%` + 65%/300ms 启动助推；同步更新 PC 工具常量。
- 2026-09-20：建立 `agent_memory/`（context / progress / bugs）与 `archive/`。
- 2026-09-20：用户提出整理工作区，范围未确认 → 挂起为待办第 1 条（无失败尝试，仅因范围不明暂停）。

## 验证说明
- 已验证：Keil 命令行编译通过，`build_log.txt` 显示 `Code=7636 RO-data=1788 RW-data=104 ZI-data=1744`、`0 Error(s), 0 Warning(s)`（AC5 V5.06 update 5）。
- 已验证（实机）：BLE 全链路通 —— 扫描/按地址直连、连接耗时 14.93s、MTU 517、`RUN30,3000` 收到 `OK` 与 3.47s 后的 `DONE`，往返约 130ms。
- 已验证：模块身份判据（`BTHLE` 枚举 + FFE0 服务 + 无 SPP 实例 + `hc05V2.3_le` 固件号）多项互相印证。
- 未验证：`MOTOR_DUTY_MIN=280` 是否为真实可维持下限 —— 该值来自设定而非标定实测，需用 `TUNE` 复核。
- 未验证：`TUNE` 标定流程本身的实机完整走通（按按键标记 → floor 生效 → 1 档变慢）尚未记录实测结果。
- 未验证：长时长命令（接近 600000ms 上限）与连续多次 `RUN` 的稳定性未记录实测。
- 未验证：上位机在"连接失败后重置蓝牙栈"这条兜底路径上的表现未做系统性复测。
